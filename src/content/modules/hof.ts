import type { Module } from '../../types'

export const hof: Module = {
  id: 'hof',
  order: 8,
  title: 'Методы массивов',
  emoji: '🧰',
  summary: 'map, filter, reduce, find, sort — обработка списков в функциональном стиле.',
  tasks: [],
}
